docker service create \
    --replicas 1 \
    --restart-condition any \
    --restart-max-attempts 10 \
    --network external_nw \
    --name monitor \
    --env 'SBUS_ENDPOINT=Endpoint=sb://...' \
    --env 'SCHEDULE=* * * * *' \
    --publish=3001:3001 \
    kthse/integral-monitor:latest
